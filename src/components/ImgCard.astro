---
const { image, navn, data } = Astro.props;
console.log(data);
---

<!-- <div class="img_container"> -->
<img src={data.img} alt={data.navn} />
<!-- </div> -->

<style>
  .img_container {
    position: relative;
    width: 100%;
    overflow: hidden;
  }

  img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: contain; /* Ensure the entire image is visible within its container */
  }
</style>
